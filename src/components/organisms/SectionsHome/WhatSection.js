import React, {Fragment} from 'react'

//import material ui components
import {Grid, Box, Typography} from '@material-ui/core'
import PaperFormContainer from '../../atoms/paper/PaperContainer'
const WhatSection = () => {
    return (
        <Grid container spacing={0} justifyContent="center" id="what-section" style={{marginTop:250}}>
            <Grid item xs={6}>
                que es esto
            </Grid>
            <Grid item xs={4}>
                
            </Grid>
        </Grid>
    )
}

export default WhatSection
