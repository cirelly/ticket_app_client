import React from 'react';
import ListItemText from '@material-ui/core/ListItemText';

const ListWrapper = ({text}) => {



    return (
        <ListItemText primary={text} />
    )

}


export default ListWrapper;